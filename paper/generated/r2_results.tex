On the event-triggered panel the denominator contains all 593 planned tasks: 497 reached a triggered checkpoint and 96 produced no trigger before termination. Cross-draw control reaches 56.24\% success against 54.97\% for continuation and 54.30\% for the best fixed repair. It therefore does not separate from continuation by 1.26 percentage points (floorplan-bootstrap 95\% interval [$-$0.35, 2.83]; group sign-flip $p=0.1579$; Holm-adjusted $p=0.1579$) and stands against the best fixed repair, a pre-specified secondary contrast that the panel is not sized to resolve, by 1.94 points ([$-$0.17, 4.17]; adjusted $p=0.0855\text{ (unadjusted)}$).

It selects continuation on 298 of 497 triggered prefixes and the warning on 36, the relocation message on 163, leaving the remaining arms unused. It fires on 40.0\% of triggered prefixes and records 52 draw-level recoveries against 37 disruptions, versus 105 and 113 for the unconditional repair. Tables~\ref{tab:r2-policies} and~\ref{tab:r2-contrasts} report every frozen policy and contrast, including the comparison-only and failure-risk learners that instantiate the two closest external objectives inside this interface.
