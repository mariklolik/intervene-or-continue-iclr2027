[project]
name = "intervene-or-continue"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "joblib==1.6.0",
  "numpy==2.5.3",
  "scikit-learn==1.9.0",
]

[tool.pytest.ini_options]
testpaths = ["tests", "extension", "controller"]
python_files = ["test_*.py"]
